## Why POST is safer?

POST bodies are not in browser history

POST bodies are not logged by API gateways by default

POST bodies never appear in referrer headers

POST bodies are not part of URLs

POST bodies can be encrypted again (JWE) if needed

This gives you maximum HIPAA safety with minimal dev effort.